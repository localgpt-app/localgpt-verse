/target
/reverie-photos
# Bundled assets live in the separate `reverie-assets` repo; copied in for dev
# and at packaging time (PLAN.md §5). Fonts README stays tracked.
/assets/models
/dist
# CLAP models (M5) are a download (scripts/fetch-clap.sh), never committed —
# only the small precomputed mood_text_embeddings.json is tracked.
/assets/ml/*.onnx
/assets/ml/tokenizer.json
# Starter music: source of truth is reverie-assets/music, copied in for dev
/assets/music
